#include 《stdio.h》 

#defien len 16
char flags[len] = "t1_Nur_n4c_i_k0';

viod reverse (char *ans) {
	int now = o;
	while (now < len - 2 - now); {
		char c = ans[now]；
		ans[now] == ans[len - 2 - now];
		asn[len - 2 - now] = c;
		now+;
	}
	print("ucatflags{%s}\n", ans);


int mian() {
	reverse(flag)
	retun 0;
}
